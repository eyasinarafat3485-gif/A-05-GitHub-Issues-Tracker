1) Ans: ★ Var: We do not use var at the advanced level, because it is very difficult to find/define a specific variable in long code using var.
★ Let: value of let can be changed.
★ Const: The value of const cannot be changed. 

2) Ans: The spread operator in JavaScript is a syntax that expands iterables or object properties into individual elements. it is part of in ES6,

3) Ans: ★ Map is a js ES6 concepts. It is transform each element in an array into a new value, create a new array of the same length with the transformed elements. 
★ Filter is a js ES6 concepts. It is select a subset of elements from an array based on a condition.
★ ForEach is an important topics in js. It is execute a function on each element of an array for side effects we use this , such as (logging to the console, saving data to a database).

4) Ans: Arrow function is a concise alternative syntax for writing function expressions in JavaScript, introduced in ECMAScript (ES6). 

5) Ans: Template literals are a modern and flexible way to create strings in JavaScript. For this, backticks are used, through this, dynamic value / card can be obtained very easily.